name: Build godotcoacd for iOS

on:
  workflow_dispatch:

jobs:
  ios:
    name: iOS (${{ matrix.arch }} / ${{ matrix.target }})
    runs-on: macos-latest

    strategy:
      fail-fast: false
      matrix:
        arch: [ios_device, ios_simulator]
        target: [template_debug, template_release]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Configure CMake for iOS
        run: |
          cmake -S . -B build \
            -G "Unix Makefiles" \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_SYSROOT=$([ "${{ matrix.arch }}" = "ios_device" ] && echo "iphoneos" || echo "iphonesimulator") \
            -DGODOTCPP_TARGET=${{ matrix.target }} \
            -DCMAKE_TRY_COMPILE_TARGET_TYPE=STATIC_LIBRARY \
            -DOPENVDB_BUILD_VDB_PRINT=OFF


      - name: Build
        run: cmake --build build --target godotcoacd --parallel $(( $(sysctl -n hw.ncpu) - 1 ))

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: godotcoacd-ios-${{ matrix.arch }}-${{ matrix.target }}
          path: addons/godotcoacd/bin/*
